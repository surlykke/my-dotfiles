#!/usr/bin/env bash

killall swayidle

export MARKER=$XDG_RUNTIME_DIR/count-down-marker
exec swayidle -w \
	before-sleep "lockScreen" \
	timeout 180 "lockScreen 3 10" \
	timeout 600 "niri msg action power-off-monitors"

