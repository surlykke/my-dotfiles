#!/usr/bin/env bash
SECS=$1
MARKER=$2
while [ $SECS -gt 0 ]; do 
	notify-send -i dialog-information "Låser om" "$SECS sek." -r 32425262 -t 1200
	sleep 1
	if [ ! -e $MARKER ]; then exit; fi 
	SECS=$(( $SECS - 1 ))
done
rm $MARKER
